import org.apache.tools.ant.filters.ReplaceTokens

plugins {
  id 'java'
  id 'eclipse'
  id 'idea'
  id 'com.github.johnrengelman.shadow' version '1.2.4'
}

group = 'com.codeski'
version = '1.4.1-SNAPSHOT'
sourceCompatibility = '1.8'

processResources {
  filesNotMatching("**/*.png") {
    filter ReplaceTokens, tokens: [
      "version": project.property("version")
    ]
  }
}

dependencies {
  compile 'com.codeski:NBT:0.1.2'
  compile 'org.bukkit:bukkit:1.8-R0.1-SNAPSHOT'
  compile 'org.bstats:bstats-bukkit:1.1'
  compile 'org.dynmap:DynmapCoreAPI:1.9.4'
}

repositories {
  maven { url 'https://kovuthehusky.com/nexus/content/groups/public/' }
  maven { url 'http://hub.spigotmc.org/nexus/content/groups/public/' }
  maven { url 'http://repo.bstats.org/content/repositories/releases/' }
  maven { url 'http://repo.mikeprimm.com/' }
}

shadowJar {
  dependencies {
    include(dependency('com.codeski:NBT'))
    include(dependency('org.bstats:bstats-bukkit'))
  }
  relocate 'org.bstats', 'com.codeski'
}

jar.finalizedBy(shadowJar);
